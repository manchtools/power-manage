// ExportService (SPEC-003 §3.2): one-way OTLP export to SIEMs, structurally
// PII-barriered (OPS-2, SPEC-016). Optional binary.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: powermanage/v1/export.proto

package powermanagev1connect

import (
	connect "connectrpc.com/connect"
	_ "github.com/manchtools/power-manage/contract/gen/go/powermanage/v1"
	http "net/http"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ExportServiceName is the fully-qualified name of the ExportService service.
	ExportServiceName = "powermanage.v1.ExportService"
)

// ExportServiceClient is a client for the powermanage.v1.ExportService service.
type ExportServiceClient interface {
}

// NewExportServiceClient constructs a client for the powermanage.v1.ExportService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewExportServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ExportServiceClient {
	return &exportServiceClient{}
}

// exportServiceClient implements ExportServiceClient.
type exportServiceClient struct {
}

// ExportServiceHandler is an implementation of the powermanage.v1.ExportService service.
type ExportServiceHandler interface {
}

// NewExportServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewExportServiceHandler(svc ExportServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	return "/powermanage.v1.ExportService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedExportServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedExportServiceHandler struct{}
