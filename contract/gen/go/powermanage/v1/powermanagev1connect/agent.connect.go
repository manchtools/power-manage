// AgentService (SPEC-003 §3.2): the ONE bidirectional agent stream —
// Hello/Welcome, signed command delivery, signed sync manifest, result
// upload, artifact fetch by digest [WIRE-28] — over mTLS via the gateway.
//
// The sync manifest is NOT a frame: it rides SignedCommand with
// command_type "sync-manifest" ([WIRE-15/26]) — a second manifest carriage
// would be a second freshness path. The stream carries no ack frames:
// delivery reliability is control's work-table re-push (GW-4, SPEC-012)
// plus the agent's durable result buffer (AG-10, SPEC-013).

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: powermanage/v1/agent.proto

package powermanagev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/manchtools/power-manage/contract/gen/go/powermanage/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AgentServiceName is the fully-qualified name of the AgentService service.
	AgentServiceName = "powermanage.v1.AgentService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AgentServiceStreamProcedure is the fully-qualified name of the AgentService's Stream RPC.
	AgentServiceStreamProcedure = "/powermanage.v1.AgentService/Stream"
)

// AgentServiceClient is a client for the powermanage.v1.AgentService service.
type AgentServiceClient interface {
	Stream(context.Context) *connect.BidiStreamForClient[v1.AgentFrame, v1.ServerFrame]
}

// NewAgentServiceClient constructs a client for the powermanage.v1.AgentService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAgentServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AgentServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	agentServiceMethods := v1.File_powermanage_v1_agent_proto.Services().ByName("AgentService").Methods()
	return &agentServiceClient{
		stream: connect.NewClient[v1.AgentFrame, v1.ServerFrame](
			httpClient,
			baseURL+AgentServiceStreamProcedure,
			connect.WithSchema(agentServiceMethods.ByName("Stream")),
			connect.WithClientOptions(opts...),
		),
	}
}

// agentServiceClient implements AgentServiceClient.
type agentServiceClient struct {
	stream *connect.Client[v1.AgentFrame, v1.ServerFrame]
}

// Stream calls powermanage.v1.AgentService.Stream.
func (c *agentServiceClient) Stream(ctx context.Context) *connect.BidiStreamForClient[v1.AgentFrame, v1.ServerFrame] {
	return c.stream.CallBidiStream(ctx)
}

// AgentServiceHandler is an implementation of the powermanage.v1.AgentService service.
type AgentServiceHandler interface {
	Stream(context.Context, *connect.BidiStream[v1.AgentFrame, v1.ServerFrame]) error
}

// NewAgentServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAgentServiceHandler(svc AgentServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	agentServiceMethods := v1.File_powermanage_v1_agent_proto.Services().ByName("AgentService").Methods()
	agentServiceStreamHandler := connect.NewBidiStreamHandler(
		AgentServiceStreamProcedure,
		svc.Stream,
		connect.WithSchema(agentServiceMethods.ByName("Stream")),
		connect.WithHandlerOptions(opts...),
	)
	return "/powermanage.v1.AgentService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AgentServiceStreamProcedure:
			agentServiceStreamHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAgentServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAgentServiceHandler struct{}

func (UnimplementedAgentServiceHandler) Stream(context.Context, *connect.BidiStream[v1.AgentFrame, v1.ServerFrame]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("powermanage.v1.AgentService.Stream is not implemented"))
}
