name: ci
on:
  pull_request:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: ./scripts/verify.sh
  conventions:
    runs-on: ubuntu-latest
    steps:
      - run: ./scripts/check-conventions.sh commits a..b
